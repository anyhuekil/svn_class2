package javaexp.a06_object.access.story;

import javaexp.a06_object.access.story.friendship.WoodCutter;

public class Hunter {
	void askWhereDeer(){
		WoodCutter w; // public 선언가능
		w= new WoodCutter(); // 생성자 접근도 가능.
//		System.out.println(w.cacheDeer);
		// cacheDeer의 필드의 접근범위가 default 이기에.
		//cacheClothe의 필드의 접근범위가 private이기에
//		System.out.println(w.cacheClothe);
		// public은 범위가 가장 넓은 것으로 외부 패키지의
		// 클래스도 import만 하면 사용 가능하다.
		System.out.println(w.weddingAnnounce);
	}
}
